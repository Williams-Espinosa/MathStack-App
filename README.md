
  # VIEW MOBILE

  This is a code bundle for VIEW MOBILE. The original project is available at https://www.figma.com/design/MSWK4jo3uMLrtYdjggLzvQ/VIEW-MOBILE.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # MathStack-App PWA
